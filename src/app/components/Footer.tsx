import SocialButtons from './SocialButtons'

export default function Footer() {
  return (
    <footer className="footer fixed bottom-0 left-1/2 transform -translate-x-1/2 text-center py-2 z-[100]">
      <SocialButtons />
    </footer>
  )
}